import { EnConstruction } from "@/components/layout/en-construction";

export const metadata = { title: "Templates emails" };

export default function Page() {
  return (
    <EnConstruction
      etape={17}
      titre="Templates emails"
      description="Bibliothèque de templates pour la prospection à froid, les relances, les emails post-RDV."
    />
  );
}
